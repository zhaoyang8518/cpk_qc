# cpk_qc 战略演进路线图 (Roadmap)

> 修订于 2026-05-20。从「单体演进」调整为「双程序 + 共享数据层」架构，优先补齐 SPC 过程控制能力。

---

## 🏗️ 架构决策：双程序 + 共享数据层

```
┌──────────────────┐     ┌──────────────────┐
│   cpk_qc (现有)   │     │  SPC 控制台 (新增) │
│  单次抽样 CPK 分析 │     │  历史趋势 & 控制图  │
│  直方图 + 正态拟合 │     │  Western Electric │
│  RF 热力图        │     │  判异规则引擎      │
└────────┬─────────┘     └────────┬─────────┘
         │                        │
         └──────────┬─────────────┘
                    │ sqlx / tokio-postgres
                    ▼
         ┌──────────────────┐
         │ PostgreSQL       │  ← 统一数据源
         │ 直接写入 / 共享查询 │  ← 团队协作 / 追溯审计
         └──────────────────┘
```

**为什么拆成两个程序**：
- cpk_qc 是「空间维度铺开」——多指标并排直方图矩阵，适合单次批次全面体检
- SPC 控制台是「时间维度纵向」——单指标跨批次趋势，适合过程监控和失控预警
- 两者交互模式、信息密度、组件树完全不同，拆开比硬塞在一起更清晰

**为什么直接写 PostgreSQL**：
- 放弃本地 SQLite 中间层，避免同步一致性、冲突处理和备份链路复杂化
- cpk_qc 导入解析完成后直接写入 PostgreSQL，SPC 控制台读取同一数据源
- 数据天然支持团队共享、集中备份、权限控制、追溯审计和后续 BI/AI 查询

---

## 🚀 演进阶段

```
[ 阶段一：单点桌面分析 ] ──> ✅ 已完成 (v0.1.1)
         │
         ▼
[ 阶段二：数据沉淀基础设施 ] ──> PostgreSQL 存储 + cpk_qc 导入存档
         │
         ▼
[ 阶段三：SPC 过程控制 ] ──> 独立 SPC 程序：控制图 + 判异规则 + 告警
         │
         ▼
[ 阶段四：高级质量分析 ] ──> 柏拉图 + MSA + 非正态 + 供应商记分卡
         │
         ▼
[ 阶段五：AI 与大模型 ] ──> ChatBI + 黄金批次 + 工艺参数推荐
```

---

## 📊 Phase 1: 数据沉淀基础设施（当前里程碑）

### 目标
让 cpk_qc 从「导一次看一次」变成「导一次存下来、随时回头看」。

### 核心任务
- **PostgreSQL 数据模型设计**：imports → test_metrics → indicators → measurements 四表结构，覆盖每次导入的完整数据
- **cpk_qc 改造**：导入解析完成后直接写入 PostgreSQL，底部 SheetNav 旁边增加「历史记录」入口
- **连接配置与可用性处理**：提供 PostgreSQL 连接配置、连通性检测、写入失败提示和重试入口
- **集中备份机制**：依赖 PostgreSQL 备份策略，不再导出本地 `.db` 副本

### 技术栈
- `sqlx` + `tokio-postgres`（统一 PostgreSQL 数据访问）
- PostgreSQL migrations 管理 schema 演进
- 连接信息通过 Tauri 配置/安全存储持久化

---

## 📈 Phase 2: SPC 过程控制独立程序

### 目标
从「事后分析」升级为「过程监控」——产线运行中实时判断失控信号。

### 核心功能
- **控制图**：X-Bar R、X-Bar S、I-MR，ECharts 渲染，支持按检测项 + 时间段筛选
- **Western Electric 八大判异规则引擎**：
  - 连续 7 点同侧 / 连续 6 点递增递减 / 连续 14 点交替 / 2/3 超出 2σ / 4/5 超出 1σ / 15 点集中在 1σ / 8 点均在 1σ 外 / 连续 3 点中 2 点超出 2σ
  - 违规点红色标注，自动生成判异报告
- **失控告警**：桌面通知 + 可配置 webhook（企业微信 / 飞书）
- **数据源**：直接从 PostgreSQL 读取 cpk_qc 历史导入数据

### 技术栈
- Vite + React 18 + TypeScript + ECharts（复用 cpk_qc 基础设施但独立工程）
- Tauri 2.0，通过 PostgreSQL 查询读取共享数据
- 判异规则用 Rust 实现（`cpk_core` 扩展或新建 `spc_rules` crate）

---

## 🔍 Phase 3: 高级质量分析功能

### 缺陷帕累托分析（低复杂度，快速上线）
- 按缺陷类型排列频次 + 累计百分比折线
- 分层柏拉图（按产线 / 班次 / 机台 / 供应商切片）
- DPMO / DPU 自动计算与仪表盘

### 量测系统分析 MSA（低-中复杂度）
- Gage R&R：%Study Variation、%Tolerance、ndc
- 偏倚与线性分析
- 量具能力指数 Cgk

### 非正态分布支持（中复杂度）
- Anderson-Darling 正态性检验
- Box-Cox 变换自动 lambda 搜索
- 非正态 CPK 计算（ISO 22514-4 百分位数法）
- Weibull / 对数正态 / Gamma 分布拟合

### 供应商质量记分卡（中复杂度）
- 按供应商聚合批次合格率、CPK 趋势
- 红黄绿灯月度排名
- 入料检验跳批规则建议（GB/T 2828.1）

---

## 🌐 Phase 4: 全生命周期追溯与数字孪生

- **PCBA 质量护照**：输入 SN，跨工序（SMT / AOI / ICT / FCT）全景追溯
- **微变异累积预测**：公差叠加失效风险评估

---

## 🤖 Phase 5: 工业大模型 Agent 与智能问答

- **ChatBI**：自然语言查询品质数据，秒级生成趋势简报
- **黄金批次挖掘**：ML 反推最佳工艺窗口
- **动态控制界限**：根据环境变量自适应调整 SPC 参数

---

## 🛠️ 技术栈演进

| 层面 | 当前 (v0.1.1) | Phase 1-2 | Phase 3+ |
|------|:-----------:|:---------:|:--------:|
| **桌面端** | Tauri 2.0 + React 18 | 同左 | 同左 |
| **本地存储** | 无（内存态） | 无 | DuckDB 可选（分析缓存） |
| **远端存储** | — | PostgreSQL | PostgreSQL / ClickHouse |
| **数据解析** | calamine | 同左 | 同左 |
| **图表** | ECharts 5.5 | 同左 | 同左 |
| **判异引擎** | — | Rust crate | 同左 |
| **AI 集成** | — | — | LLM API / 私有化模型 |
