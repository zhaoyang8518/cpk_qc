# 工业级过程能力分析 (SPC) 与 CPK 统计算法白皮书
**文档版本**：v1.0.0  
**适用对象**：质量工程师、客户审核专家、生产管理层、数据分析师  
**权威理论依据**：精益六西格玛 (Lean Six Sigma) 质量工程标准、Quality Assurance Solutions 统计规范

---

## 1. 执行摘要 (Executive Summary)
本白皮书旨在阐明 `cpk_qc` (CPK Quality Controller) 工业级桌面应用背后核心统计引擎的数学原理、图表呈现规范以及智能诊断逻辑。本系统严格遵循国际顶尖的精益六西格玛 (Lean Six Sigma) 质量控制准则，结合大样本统计学与概率密度分布理论，为工业测试数据提供极度精准、稳健且直观的过程能力评估。当面对客户审核或对统计图表产生疑问时，本报告可作为直接的权威释疑文件。

---

## 2. 核心概念与指标解析：Cp 与 Cpk 的前世今生
在质量管理中，评估一个生产过程是否具备满足客户规格的能力，核心依赖于两个核心指数：**Cp (潜在过程能力)** 与 **Cpk (实际过程能力)**。为了便于非统计专业人士理解，我们引入经典的**“车库停车理论”**进行通俗对照。

```
     【车库停车理论示意图】
     
     | <--- 车库宽度 (规格界限 USL - LSL) ---> |
     |                                        |
     |         [ 车身宽度 6σ ]                 |
     |        |---------------|               |
     |        |   汽车 (数据)  |               |
     |        |---------------|               |
     |                                        |
    LSL                   μ                  USL
```

### 2.1 均值 ($\mu$) 与标准差 ($\sigma$)
* **均值 ($\mu$, Mean)**：实际采集数据的算术平均值，代表数据的集中趋势。它决定了正态分布曲线峰顶在 X 轴上的位置。
  * *注：系统严禁将峰顶盲目固定在图表正中心，必须由真实数据计算得出，真实反映工艺中心的实际偏移情况。*
* **标准差 ($\sigma$, Standard Deviation)**：衡量数据波动的核心指标，代表数据的离散程度。它决定了曲线的“胖瘦”。在六西格玛理论中，$6\sigma$ 代表流程的自然波动宽度（即“车身宽度”）。

### 2.2 潜在过程能力指数 (Cp, Process Capability Potential)
$$\text{Cp} = \frac{\text{USL} - \text{LSL}}{6\sigma}$$
* **通俗解释**：**Cp 衡量的是“车库宽度”与“车身宽度”的比例。** 如果车库很宽，车身很窄（即波动 $\sigma$ 很小），Cp 就很高。
* **物理意义**：仅评估过程的**精密度（分散程度）**，完全不考虑车子是否停在车库正中间。
* **边界条件**：Cp 仅在**双边规格**（同时存在上限 USL 和下限 LSL）下成立。若只有单边规格，本系统严格遵守统计学规范，将 Cp 判定为 `null`（显示为 `-`），杜绝输出无数学意义的伪指标。

### 2.3 实际过程能力指数 (Cpk, Process Capability Actual)
$$\text{Cpk} = \min(\text{CPU}, \text{CPL}) = \min\left( \frac{\text{USL} - \mu}{3\sigma}, \frac{\mu - \text{LSL}}{3\sigma} \right)$$
* **通俗解释**：**Cpk 衡量的是“车子离车库墙壁最近的距离”与“半个车身宽度”的比例。** 即使车身很窄（Cp 很高），但如果司机把车停得非常靠边（均值 $\mu$ 严重偏离中心），车子依然有撞墙（超差）的风险，此时 Cpk 会大幅降低。
* **物理意义**：同时评估过程的**精密度与准确度（对中性）**，是工业界核心监控的终极指标。

---

## 3. 统计图表呈现规范与 X 轴动态缓冲算法
在图表可视化方面，许多传统软件常犯的错误是：直接将图表的 X 轴起点和终点锁死在 LSL 和 USL 上，或仅按数据的最大最小值截断。这会导致正态拟合曲线的尾部直接被切断或紧贴边框，丧失对数据全貌和潜在超差风险的观察。

本系统严格遵守 Quality Assurance Solutions 推荐的 **$1.5\sigma$ 动态缓冲划定算法**：

$$\text{X 轴最小值 (起点)} = \min(\text{LSL}, \mu - 3\sigma) - 1.5\sigma$$
$$\text{X 轴最大值 (终点)} = \max(\text{USL}, \mu + 3\sigma) + 1.5\sigma$$

```
   【X 轴动态缓冲视野示意图】
   
   <-- 1.5σ 缓冲 --> | <--- 核心监控区域 (LSL ~ USL 或 μ±3σ) ---> | <-- 1.5σ 缓冲 -->
   ------------------|---------------------------------------|------------------
   X_Min            LSL                                     USL               X_Max
```

* **算法优势**：无论数据分布多么集中或多么发散，图表左右两端始终保留 $1.5\sigma$ 的视觉缓冲区。这不仅使得正态拟合曲线呈现出极佳的流畅度与呼吸感，更能让审核专家一眼看清曲线尾部距离规格界限的“安全余量”。

---

## 4. 单边与双边规格界限的严密处理机制
在实际工业 PCBA 测试中，并非所有检测项都有双边界限。例如，接触电阻或泄漏电流往往只有上限（USL），而绝缘电阻或拉拔力往往只有下限（LSL）。

本系统的计算引擎对不同公差类型提供了极度严密的自动适配与保护：
1. **双边公差 (同时具 USL & LSL)**：同时计算 CPU 与 CPL，取两者的最小值作为 Cpk；同时输出精准的 Cp 值。
2. **单边上限 (仅具 USL)**：系统自动屏蔽 Cp 计算，将 Cpk 精准锁定为 $\text{CPU} = \frac{\text{USL} - \mu}{3\sigma}$。
3. **单边下限 (仅具 LSL)**：系统自动屏蔽 Cp 计算，将 Cpk 精准锁定为 $\text{CPL} = \frac{\mu - \text{LSL}}{3\sigma}$。

---

## 5. 六西格玛等级划分与 AI 智能诊断触发器 (Action Triggers)
为了将冰冷的统计数字转化为生产线上切实可行的优化动作，本系统内置了符合精益六西格玛准则的 **AI 诊断与行动触发引擎**。

```
                    【AI 智能诊断分流决策树】
                    
                           [ 计算 Cp 与 Cpk ]
                                   |
                     +-------------+-------------+
                     |                           |
             ( Cpk < 1.0 不合格 )         ( Cpk ≥ 1.33 良好/世界级 )
                     |                           |
            +--------+--------+          [ 维持现状，定时监控漂移 ]
            |                 |
     ( Cp ≥ 1.0 高 )   ( Cp < 1.0 低 )
            |                 |
     [ 触发均值偏移修正 ]  [ 触发减少变异修正 ]
     (机器重新对中校准)  (排查物料/设备/培训)
```

### 5.1 过程能力判定标准与 Sigma 水平 ($Z$)
系统根据短期制程能力换算公式 ($Z = 3 \times \text{Cpk}$) 自动计算精确的 Sigma 水平，并对检测项进行分级预警：
* 🔴 **不合格 ($\text{Cpk} < 1.0$, $< 3\text{ Sigma}$)**：过程变差超出规格界限，存在大量次品风险，必须立即停机整改。
* 🟡 **勉强合格 ($1.0 \le \text{Cpk} < 1.33$, $3 \sim 4\text{ Sigma}$)**：处于工业公差边缘，当工艺参数发生轻微漂移时极易超差，需密切监控。
* 🟢 **良好 ($1.33 \le \text{Cpk} < 2.0$, $4 \sim 6\text{ Sigma}$)**：满足主流工业与汽车电子制造标准，过程处于受控且具备能力的健康状态。
* 🔵 **世界级水平 ($\text{Cpk} \ge 2.0$, $\ge 6\text{ Sigma}$)**：极其卓越的过程能力，缺陷率接近零（$< 3.4\text{ DPMO}$），维持现有状态即可。

### 5.2 优化动作触发器 (Action Triggers 诊断逻辑)
当系统判定检测项能力不足（$\text{Cpk} < 1.0$）时，AI 引擎会自动比对 Cp 与 Cpk 的差异，给出极具指导意义的根因排查建议：
1. **场景 A：Cp 高，但 Cpk 低**
   * **AI 诊断结论**：`【均值偏移修正】：机器需重新校准/对中，调整均值移向规格中心。`
   * **工程指导**：说明设备本身的加工精密度极高（没有大波动），仅仅是设定的基准位置偏了。只需调整夹具定位、修改机台补偿参数或重新对刀，即可轻松将 Cpk 提升至优秀水平。
2. **场景 B：Cp 与 Cpk 均低 (或 Cp < 1.0)**
   * **AI 诊断结论**：`【减少变异修正】：工艺不稳定，需检查原材料稳定性、设备磨损或强化员工培训。`
   * **工程指导**：说明生产过程本身的变差太大（车身太宽）。此时单纯靠调机对中毫无意义，必须深入排查来料批次差异、机台轴承磨损、测试夹具接触不良或操作工手法不规范等系统性波动源。

---

## 6. 数学防溃与工业级稳健性保证 (Industrial Robustness)
在真实的工业现场数据中，常出现极端异常情况。例如，某批次 PCBA 的某项电压读数由于仪表精度限制，全部记录为 `3.300V`。在传统软件中，这会导致标准差 $\sigma = 0$，进而引发除以零的致命崩溃（程序卡死或显示 `NaN`/`Infinity`）。

本系统底层计算引擎 (`spc.ts`) 引入了最高级别的**极小值防溃屏障**：
```typescript
if (sigma > 1e-9) { 
  // 进行正常六西格玛除法计算
} else {
  // 启动异常防溃保护机制
}
```
* **稳健性表现**：当检测到标准差小于 `0.000000001` 时，系统会自动启动平滑保护逻辑，保证软件在任何极端恶劣的数据输入下均能流畅运行、绝不崩溃。
