# cpk_qc (CPK与质量控制统计分析软件) 功能说明与开发计划

## 一、 项目概述与业务背景
在电子制造与 PCBA 质量控制（QC / SPC）过程中，质量工程师通常需要对 ATE 测试机台导出的多 Sheet、多通道测试数据（如 Excel 表格）进行深入的制程能力分析（CPK）。
本项目（工程代号：`cpk_qc`）旨在基于 **Tauri + React + Tailwind CSS** 构建一款轻量级、高性能且具备现代化交互体验的桌面客户端。软件能够异步高效导入大体积 Excel 文件，自动解析提取规格上下限（USL/LSL）与单板测试数据，并通过可视化图表矩阵实时呈现多列指标的直方图分布与正态拟合曲线（CPK 红线），从而助力工程师快速识别制程变差与双峰异常，实现对异常单板的高效追溯。

---

## 二、 核心功能与交互说明

### 1. 多 Sheet Excel 导入与智能数据解析
* **高效文件加载**：调用 Tauri 原生文件选择对话框导入 Excel 文件（`.xlsx`），支持多 Sheet 大文件异步加载，主界面提供清晰的加载进度与解析动画。
* **工业级表头解析（基于 AP-735 样例规范）**：
  * **参数与规格行提取**：精准提取 Excel 表格前 9 行的统计与规格基准数据（包括 `Average`, `MAX`, `MIN`, `Stdev`, `Ca`, `Cp`, `Cpk`, `USL`, `LSL`），重点锁定第 8 行（USL 规格上限）与第 9 行（LSL 规格下限）。
  * **列名与单板数据绑定**：锁定第 10 行为指标列头（包含 `Uid`, `PCBASN` 及各项测试项目名称），从第 11 行起遍历提取每一块单板对应的测试数值。

### 2. 沉浸式视窗布局与交互设计 (UI/UX)
软件主界面采用现代工业风格的经典“分栏+多页签”布局：

```
+-------------------------------------------------------------------+
| [导入 Excel]   当前文件: AP-735_demo.xlsx         [全局设置] [导出] |
+---------------+---------------------------------------------------+
|  PCBASN 列表  |  指标图表矩阵 (2~3 个一行，响应式自适应排列)          |
|  [搜索单板...] |                                                   |
|               |  +--------------------+  +--------------------+   |
|  R.DXX6290027 |  | BLEX2402PWRXXAVG01 |  | BLEX2402PWRXXAVG04 |   |
|  R.DXX6290022 |  |   _                |  |       _            |   |
|  R.DXX6290010 |  |  | |   /\   _      |  |      / \   _       |   |
|  R.DXX6290021 |  | _|_|__/__\_|_|_    |  | ____/___\_|_|___   |   |
|  R.DXX6290006 |  | USL:12    LSL:7.5  |  | USL:12    LSL:7.5  |   |
|  (上下滚动)    |  +--------------------+  +--------------------+   |
|               |  (流畅上下滚动)                                    |
+---------------+---------------------------------------------------+
| < Sheet 1 (SPC) | Sheet 2 (RF) | Sheet 3 (Audio) >    (横向滚动)    |
+-------------------------------------------------------------------+
```

* **顶部操作栏**：提供文件导入按钮、当前加载文件路径/名称显示、全局显示设置（如图表列数切换）以及图表/数据导出按钮。
* **左侧单板面板 (PCBASN 列表)**：
  * 固定宽度（如 260px），展示当前 Sheet 下所有单板的 PCBASN 条码列表。
  * 顶部内置吸顶搜索框，支持对单板条码进行快速模糊过滤。
  * 支持虚拟滚动（Virtual Scrolling），在面对数万片单板时依然保持极致流畅。
* **右侧主体区域 (统计图表矩阵)**：
  * 占据视窗主要空间，基于响应式 CSS Grid 布局，默认每行排布 **2~3 个图表卡片**，依次向下排开，支持流畅的上下滚动浏览。
  * **单卡片构成**：
    * **卡片头部**：显示当前测试指标名称（如 `BLEX2402PWRXXAVG01`）。
    * **主图表区**：直方图柱状分布（反映频数） + 正态拟合理论曲线（红线，反映制程能力）。
    * **辅助基准线**：自动绘制虚线形式的规格上限（USL）与规格下限（LSL）。
    * **统计参数看板**：在右上角悬浮展示当前指标的 `N`（样本数）、`均值`、`标准差` 与 `CPK` 数值。
* **底部栏 (Sheet 导航栏)**：
  * 横向可滚动的 Tab 标签页，动态展示 Excel 中所有的 Sheet 名称。
  * 点击任意 Sheet 标签实时切换数据源，左侧 PCBASN 列表与右侧图表矩阵平滑重载。

### 3. 特色交互亮点：单板与图表双向联动追溯
* **单板高亮联动**：当质量工程师在左侧 PCBASN 列表中点击或悬浮某块单板条码时，右侧矩阵中所有图表将同步高亮显示该单板在对应指标直方柱中的具体位置（或以特殊标记点标出），极大地提升了工程师追踪特定缺陷单板或边缘单板的效率。

---

## 三、 技术架构与工程规范

### 1. 前端技术栈与规范
* **环境与运行库**：严格采用 **Node.js v22.22.2** 运行环境，使用 **pnpm** 作为包管理工具，保障依赖安装速度与幽灵依赖隔离。
* **核心框架**：**React 18 + TypeScript + Vite**，开启严格模式与 TS 类型静态检查。
* **UI 与样式**：采用 **Tailwind CSS**，配合前端组件库（如 Shadcn UI 或原生 Tailwind 封装组件），实现现代化、高质感的深浅色工业视窗界面。
* **图表渲染引擎**：使用 **Apache ECharts**（结合 `echarts-for-react`）。利用 ECharts 强大的双轴（Bar + Line）、MarkLine（辅助线）、MarkPoint（标记点）功能以及出色的 Canvas 大数据渲染性能，完美呈现工业直方图与拟合曲线。

### 2. 后端技术栈与规范
* **核心框架**：**Tauri 2.0 (Rust)**，利用系统原生 WebView 构建极致轻量、启动迅速的桌面应用。
* **工程工作目录 (Cargo Workspace)**：
  * 采用 Cargo Workspace 结构组织 Rust 后端代码，分为主程序 `cpk_qc` 与核心数据解析逻辑 `cpk_core` 等独立 crate，实现依赖与业务解耦。
* **国内镜像与依赖加速**：
  * 在项目根目录或 `.cargo/config.toml` 中配置国内镜像源（如清华大学、字节跳动或中科大源），确保国内开发环境下 crates 依赖的高速稳定下载。
* **Excel 解析引擎**：
  * 后端引入 Rust 社区备受好评的高性能读取库 `calamine`。`calamine` 采用纯 Rust 编写，支持内存映射与零拷贝解析，能够在极短时间内读取数十 MB 的多 Sheet 工作表，并通过 Tauri IPC (Inter-Process Communication) 将格式化后的 JSON 结构体安全传递给前端。

---

## 四、 分阶段开发计划 (Milestones & WBS)

整个项目开发周期预计为 **10~12 个工作日**，划分为 5 个核心阶段：

### Phase 1: 工程初始化与基础架构搭建 (Day 1-2)
* **任务 1.1**：配置 Node v22.22.2 与 pnpm 环境，使用 Vite 初始化 React + TypeScript 前端工程。
* **任务 1.2**：初始化 Tauri 2.0 项目，构建 Cargo Workspace 工作空间结构，并在 `.cargo/config.toml` 中配置国内加速镜像源。
* **任务 1.3**：安装并配置 Tailwind CSS 样式引擎及 Lucide 图标库，建立基础的全局配色与主题 Token。
* **任务 1.4**：引入 `echarts` 及 `echarts-for-react`，封装基础的自适应 ECharts 容器组件。

### Phase 2: 后端 Excel 高性能解析引擎开发 (Day 3-4)
* **任务 2.1**：在 Rust 工作空间中引入 `calamine` 与 `serde_json` 库。
* **任务 2.2**：编写 Excel 异步读取器，针对 AP-735 样例结构，实现对前 9 行统计/USL/LSL 参数以及第 10 行表头的精准定位与提取。
* **任务 2.3**：实现对多 Sheet 列表及单板测试数据流的封装，设计高效、清晰的 Rust -> React IPC 数据传输接口 (DTO)。
* **任务 2.4**：编写 Rust 单元测试，验证数据解析的正确性与内存消耗。

### Phase 3: 主体 UI 布局与导航系统开发 (Day 5-6)
* **任务 3.1**：实现顶部操作栏（导入按钮、文件信息、视图控制）。
* **任务 3.2**：开发底部 Sheet 标签页横向滚动导航组件，实现与 Tauri IPC 接口的交互与数据源重载。
* **任务 3.3**：开发左侧 PCBASN 列表面板，集成模糊搜索过滤功能，并引入虚拟滚动组件（如 `@tanstack/react-virtual`）保障长列表渲染性能。
* **任务 3.4**：完成主视窗三栏响应式网格框架的搭建，确保分栏拖拽与左右宽度的协调性。

### Phase 4: SPC 统计算法引擎与图表矩阵开发 (Day 7-9)
* **任务 4.1**：在前端（或后端核心库）实现直方图分箱算法（Sturges / Freedman-Diaconis 准则），自动计算各组距与频数。
* **任务 4.2**：实现正态分布概率密度函数 (PDF) 计算逻辑，生成与直方图同量级的正态拟合曲线（红线）数据点。
* **任务 4.3**：封装 ECharts 复合图表组件，集成直方图 (Bar)、拟合曲线 (Line)、USL/LSL 辅助线 (MarkLine) 及右上角统计浮窗。
* **任务 4.4**：实现右侧图表矩阵布局（每行 2~3 个卡片自适应），完成大规模图表同时加载时的渲染性能优化。

### Phase 5: 联动交互、性能调优与打包交付 (Day 10-11)
* **任务 5.1**：实现核心亮点功能——左侧 PCBASN 列表与右侧图表矩阵的“单板高亮联动追溯”交互。
* **任务 5.2**：进行整体性能调优，优化大文件导入时的防抖与 IPC 传输切片，确保界面在操作过程中的丝滑响应。
* **任务 5.3**：配置 Tauri 打包脚本 (`tauri.conf.json`)，自定义应用图标与安装包参数。
* **任务 5.4**：构建生成 macOS (`.dmg` / `.app`) 独立安装包，并完成最终交付与演示验收。

---

## 五、 测试与验收标准

1. **功能完整性验收**：
   * 成功导入多 Sheet 的 AP-735 样例 Excel 文件，底部正确渲染所有 Sheet 标签且支持横向滚动切换。
   * 左侧完美加载所有 PCBASN 列表，支持顺畅滚动与关键字精准搜索。
   * 右侧以 2~3 个一行的网格形式正确渲染出所有测试指标的统计图表。
2. **图形与数据准确性验收**：
   * 图表呈现的直方图分布形态、红线拟合走势与提供的图1、图2视觉标准完全一致。
   * 辅助线准确标示出第 8 行与第 9 行提取的 USL/LSL 数值。
   * 图表右上角显示的 N、均值、标准差与 Cpk 值与 Excel 前 9 行算出的基准数值完全吻合。
3. **性能与体验验收**：
   * 导入 30MB 以上、包含数十个 Sheet 和上万行单板数据的 Excel 文件时，应用不得出现崩溃或长达数秒的无响应白屏。
   * 左侧单板列表滚动帧率稳定在 60fps，点击单板联动右侧图表高亮的响应时间小于 100ms。